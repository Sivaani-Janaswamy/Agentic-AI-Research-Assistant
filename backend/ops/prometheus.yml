global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "research-assistant-backend"
    static_configs:
      - targets:
          - "localhost:8000"
    metrics_path: "/metrics"

# If your backend runs on another host/port in prod, replace localhost:8000 above.
